credentials.json
__pycache__
.env
storage/

# Artefact de verificare echivalenta D212 (nu intra in repo)
.d212_baseline.json

# Artefact DB de test (SQLite local) — nu intra in repo
test_data.db
# Baza SQLite locală (dev/preview vizual) — creată de serverul de preview, nu intră în repo
data.db
# Unelte de preview vizual local (server Flask + seed-uri demo) — dev-only, nu intră în repo
_preview/
